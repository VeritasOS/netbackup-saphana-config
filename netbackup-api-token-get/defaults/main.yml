---

# NBU API Connetion parameters
nbu_content_type: application/vnd.netbackup+json;version=3.0
nbu_api_port: '1556'
base_api_url: "{{ 'https://' + master_server + ':' + nbu_api_port + '/netbackup/' }}"

# NBU Credentials - Temp, need to move to Vault
# master_server: "{{ groups['master_servers'][0] }}"


